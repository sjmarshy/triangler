import Triangler from '../components/triangler';

export default class Index extends React.Component {
    render() {
        return (
            <Triangler src="/static/test.jpg" triWidth={40} triHeight={75} />
        );
    }
}
